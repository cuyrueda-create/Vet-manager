// CAMBIA la IP por la de tu computadora en la red local
// En Windows: abre cmd y escribe `ipconfig` para ver tu IP
const API_CONFIG = {
  baseURL: 'http://10.90.41.69:5000',
};

export default API_CONFIG;
